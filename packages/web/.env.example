# ============================================================================
# Celebrai Web — variáveis de ambiente do frontend (Vite)
# ============================================================================
# URL base da API. Em desenvolvimento, deixe vazio para usar o proxy do Vite
# (mesmo origin, sem CORS). Em produção, aponte para a URL pública da API.
VITE_API_URL=
